apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.registerApi.name }}-service
spec:
  selector:
    app: {{ .Values.registerApi.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
